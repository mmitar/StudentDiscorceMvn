--Create Schema `studisc`
CREATE SCHEMA studisc;

--Create Table `users`
CREATE TABLE studisc.users
(
	ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY(START WITH 1, INCREMENT BY 1),
	USERNAME VARCHAR(30) NOT NULL,
	PASSWORD VARCHAR(30) NOT NULL,
	FIRSTNAME VARCHAR(30) NOT NULL,
	LASTNAME VARCHAR(30) NOT NULL,
	EMAIL VARCHAR(30) NOT NULL,
	PHONE VARCHAR(14) NOT NULL,
	PERMISSION VARCHAR(100) NOT NULL
);

--Create Index & Primary Key for `users.ID`
CREATE UNIQUE INDEX SQL181104194724830 ON studisc.users (ID ASC);
ALTER TABLE studisc.users ADD CONSTRAINT USERS_PRIMARY_KEY PRIMARY KEY (ID);

--Create Table `courses`
CREATE TABLE studisc.courses (
		ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY(START WITH 1, INCREMENT BY 1),
		COURSE_ID VARCHAR(10) NOT NULL,
		TITLE VARCHAR(200) NOT NULL,
		DESCRIPTION VARCHAR(200) NOT NULL,
		MAJOR VARCHAR(200) NOT NULL,
		CLASS_LOCATION VARCHAR(200) NOT NULL,
		CLASS_TIMES VARCHAR(200) NOT NULL,
		TUTOR_TIMES VARCHAR(200) NOT NULL,
		IMAGE VARCHAR(500) NOT NULL
	);

--Create Index & Primary Key for `courses.ID`
CREATE UNIQUE INDEX SQL181104194724850 ON studisc.courses (ID ASC);
ALTER TABLE studisc.courses ADD CONSTRAINT COURSES_PRIMARY_KEY PRIMARY KEY (ID);